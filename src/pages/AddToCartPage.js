import React from 'react';
import { Container } from '@material-ui/core';
import AddToCart from '../components/AddToCart';

const AddToCartPage = ({ addToCart }) => {
  return (
    <Container>
    sdfjlskd
      <AddToCart addToCart={addToCart} />
    </Container>
  );
};

export default AddToCartPage;
